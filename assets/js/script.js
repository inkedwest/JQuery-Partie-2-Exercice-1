$(function(){ // On appelle la fonction
    $(document).ready(function(){ // On relie le HTML
        $('#clickMe').click(function(){ // Fonction au clic
            alert('Mes salutations preu chevalier !'); // Boîte de dialogue avec message à afficher
        });
    });
});
